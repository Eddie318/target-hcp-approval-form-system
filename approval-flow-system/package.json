{
  "name": "approval-flow-system",
  "private": true,
  "version": "0.1.0",
  "description": "Approval flow system monorepo (API + mock + infra).",
  "workspaces": [
    "apps/api",
    "apps/mock"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "build": "npm run build --workspaces",
    "ci": "npm run lint && npm run test && npm run build",
    "prisma:generate": "npx prisma generate",
    "prisma:migrate": "npx prisma migrate dev",
    "contract-test": "node scripts/contract-test.js",
    "import:assignments": "node scripts/import-assignments.js",
    "import:pharmacy-whitelist": "node scripts/import-pharmacy-whitelist.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "csv-parse": "^6.1.0",
    "dotenv": "^17.2.3"
  }
}
